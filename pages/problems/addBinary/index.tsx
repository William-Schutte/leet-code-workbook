import { Heading, Text } from "@chakra-ui/react";
import Layout from "../../layout";

export default function AddBinary() {
  return (
    <Layout>
      <Heading>Add Binary</Heading>
      <Text>
        Given two binary strings a and b, return their sum as a binary string.
      </Text>
    </Layout>
  );
}
