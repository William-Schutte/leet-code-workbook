import { Heading, Text } from "@chakra-ui/react";
import Layout from "../../layout";

export default function SpiralMatrix() {
  return (
    <Layout>
      <Heading>Spiral Matrix</Heading>
      <Text>
        Given an m x n matrix, return all elements of the matrix in spiral
        order.
      </Text>
    </Layout>
  );
}
